# add 
available = available ${time-hint}

#login
login = login ${username-hint} pw ${password-hint} as ${role-hint}

#register
register = register ${username-hint} pw ${password-hint} as ${role-hint}

# add
add = add ${amount-hint}

# proj
proj = proj ${years-hint}

# post add
postAdd = postadd ${content-hint}

# post delete
postDelete = postdelete ${index-hint}

# post comment
postComment = postcomment ${content-hint} on ${index-hint}

# post list
postList = postlist ${list-hint}

# schedule
schedule = schedule ${meeting-hint} with ${usernames-hint}

# generate pdf
generate = generate ${document-hint}